<%= form_with(model: fillin_test) do |form| %>
  <% if fillin_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fillin_test.errors.count, "error") %> prohibited this fillin_test from being saved:</h2>

      <ul>
        <% fillin_test.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
