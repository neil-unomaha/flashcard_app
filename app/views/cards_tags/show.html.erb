<p id="notice"><%= notice %></p>

<p>
  <strong>Card:</strong>
  <%= @cards_tag.card_id %>
</p>

<p>
  <strong>Tag:</strong>
  <%= @cards_tag.tag_id %>
</p>

<%= link_to 'Edit', edit_cards_tag_path(@cards_tag) %> |
<%= link_to 'Back', cards_tags_path %>
