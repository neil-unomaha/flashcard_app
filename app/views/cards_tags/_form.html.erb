<%= form_with(model: cards_tag) do |form| %>
  <% if cards_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cards_tag.errors.count, "error") %> prohibited this cards_tag from being saved:</h2>

      <ul>
        <% cards_tag.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :card_id %>
    <%= form.text_field :card_id %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.text_field :tag_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
