<%= form_with(model: fillin_category) do |form| %>
  <% if fillin_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fillin_category.errors.count, "error") %> prohibited this fillin_category from being saved:</h2>

      <ul>
        <% fillin_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :fillin_domain_id %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
